import os # import module
import sqlite3 # import library
import glob # import package glob after pip install glob
print(os.listdir())
print(os.path.dirname(os.__file__))
# print(os.listdir(os.path.dirname(os.__file__)))
